node_modules
dist
.astro
.git
.gitignore

# Exclude env files at any level (root, remark42/, etc.) so no secrets
# end up baked into the published image via `COPY . .`.
**/.env
**/.env.*

PLAN
README.md
*.log
.DS_Store
